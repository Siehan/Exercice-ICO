// SPDX-License-Identifier: MIT

pragma solidity ^0.8.4;
/*
import "./SmyToken.sol";
import "./SmyTokenICO.sol";

contract Calculator {
    SmyToken private _token;
    uint256 private _totalSupply;
    uint256 public constant INITIAL_SUPPLY_ = 1000000 * 10**18; // cap is the number of tokens that we want to sale
    uint256 public constant RATE_ = 100 * 10**18; // is the number of tokens that will be sold per 1 Ether (1 Ethereum)
    uint256 public tokensSold = 0; // the number of tokens that have been sold
    uint256 public constant TOKEN_RESERVE_ = 1000000 * 10**18; // is the number of tokens that you want to keep for yourself or company.
    uint256 public remainingTokens = 0; // is the number of tokens remaining that can be sold

    function add(int256 nb1, int256 nb2) public payable returns (int256) {
        _token.transferFrom(msg.sender, _owner, _totalSupply);
        return nb1 + nb2;

        // insérer votre code ici
    }

    function add(uint256 nb1, uint256 nb2) public payable returns (uint256) {
        return nb1 + nb2;
    }

    function mul(uint256 nb1, uint256 nb2) public payable returns (uint256) {
        return nb1 * nb2;
    }

    function div(uint256 nb1, uint256 nb2) public payable returns (uint256) {
        require(nb2 != 0, "Calculator can not divide digit 0");
        return nb1 / nb2;
    }

    function sub(uint256 nb1, uint256 nb2) public payable returns (uint256) {
        return nb1 - nb2;
    }

    function modulo(uint256 nb1, uint256 nb2) public payable returns (uint256) {
        return nb1 % nb2;
    }
}
*/
